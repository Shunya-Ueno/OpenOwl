# backend/.env.test
# 統合テスト用。**テスト専用の Supabase プロジェクト**を指すこと(本番を指さない)。
# 実値はコミットしない(.gitignore 済み)。CI では GitHub Actions Secrets から注入する。

# テスト用プロジェクトの URL
TEST_SUPABASE_URL=

# publishable key(旧 anon key)。RLS 越しの検証に使う。
TEST_SUPABASE_PUBLISHABLE_KEY=

# secret key(旧 service_role key)。テストユーザーの作成・削除とテストデータの整理に使う。
# **本番プロジェクトのキーを絶対に入れないこと。**
TEST_SUPABASE_SECRET_KEY=
